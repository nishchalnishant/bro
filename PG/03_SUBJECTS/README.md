# Subjects

Each subject folder should contain the same internal structure:

- `Notes`
- `MCQs`
- `PYQs`
- `Revision`
- `Mistakes`
- `Images`

Suggested rule:

- Keep raw source material in `02_RESOURCES`.
- Keep processed learning inside the subject folder.
